# Environment
python -m venv .venv
.venv\Scripts\activate  [Su linux -> source .venv/bin/activate]
                        [which python       Deve puntare a .venv per assicurarti che è attivo]

# Requirements
pip freeze > requirements.txt
pip install -r requirements.txt

# Eseguire come amministratore il programma
sudo .venv/bin/python main.py

############################# OLLAMA #############################
Ping verso window (VmNet1):
ping -c1 192.168.174.1      [Indirizzo VmNet1 ma con ultima cifra pari a 1 invece di 0]

Per ollama su win:
$env:OLLAMA_HOST="0.0.0.0:11434"
ollama serve

Poi qui:
curl http://192.168.174.1:11434/api/tags


############################# NFQUEUE #############################
sudo apt install build-essential python3-dev libnetfilter-queue-dev
pip install NetfilterQueue

sudo iptables -A OUTPUT -p tcp -d 192.168.20.10 --dport 80 -j NFQUEUE --queue-num 1
sudo iptables -A INPUT -p tcp -s 192.168.20.10 --sport 80 -j NFQUEUE --queue-num 1


############################# MITMPROXY #############################
Su altro terminale:
mitmdump -s mitm_addon.py

############################# TCPDUMP SU MINT #############################
